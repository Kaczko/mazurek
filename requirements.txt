# Install torch from the CPU wheel index if you do not need CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
numpy>=1.24
pillow>=9.0
matplotlib>=3.6
scikit-learn>=1.2
joblib>=1.2
